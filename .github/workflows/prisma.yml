
name: prisma
on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Run prisma client generate
      - uses: actions/setup-node@v1
        with:
          node-version: 16
      - run: |
          npx prisma generate
